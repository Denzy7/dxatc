#add_library(add_ai_plane SHARED add_ai_plane.c)
file(GLOB tests *.c)
foreach(test ${tests})
    get_filename_component(test_tgt ${test} NAME_WE)
    add_executable(${test_tgt} ${test})
    target_link_libraries(${test_tgt} PRIVATE dxatc-engine dxatc-utils)
endforeach()
